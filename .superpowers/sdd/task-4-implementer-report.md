# Task 4 Implementer Report

## Result

Implemented protocol-specific WriterState and snapshot V2 from base `340661be2`.
The three-argument `WriterStateManager` remains V0 compact. The four-argument
constructor selects one map and lifecycle contract: `Map<Long, WriterStateEntry>` for
V0 or `Map<WriterKey, FencedWriterStateEntry>` for V1. The unselected map is `null` and
is never allocated or populated.

## RED / GREEN

- Baseline worktree was clean at `340661be2135c530d4104e7e756355251fa3aaa3`.
- The brief's exact reactor command stopped in the root project because Surefire found
  no root tests. Re-running with `-DfailIfNoTests=false` reached `fluss-server` and
  failed test compilation with 33 expected missing-symbol errors for the protocol
  constructor, V1 state classes/APIs, and V2 serde. This was the RED observation.
- Production compile then passed after the minimal implementation.
- Focused GREEN: `WriterStateManagerTest` 29 tests and
  `WriterSnapshotMapJsonSerdeTest` 6 tests, 35 total, 0 failures/errors/skips.
- Full requested regression GREEN: the two focused classes plus `LogTabletTest`, 52
  total tests, 0 failures/errors/skips. The final post-review run used:

  ```bash
  mvn -o -Dmaven.repo.local=.cache -pl fluss-server -am \
    -Dspotless.check.skip=true -DfailIfNoTests=false \
    -Dtest=WriterStateManagerTest,WriterSnapshotMapJsonSerdeTest,LogTabletTest test
  ```

## V0 Literal Fixture

Fixture provenance: captured from the deterministic V0 snapshot representation at
base `340661be2`, using the existing three-argument manager with writer ID `5`, batch
sequence `0`, base/last offset `8`, offset delta `0`, timestamp `9`, and map end offset
`9`. The expected value is a literal test constant; it is not generated by the modified
serde.

- Length: 144 bytes
- UTF-8 bytes:

  ```json
  {"version":1,"writer_id_entries":[{"writer_id":5,"last_batch_sequence":0,"last_batch_base_offset":8,"offset_delta":0,"last_batch_timestamp":9}]}
  ```

- Hex:

  ```text
  
  ```

The snapshot-file test writes through `WriterStateManager.takeSnapshot()` and compares
the complete file contents to these literal bytes.

## Files

- Created `fluss-server/src/main/java/org/apache/fluss/server/log/FencedWriterStateEntry.java`
- Created `fluss-server/src/main/java/org/apache/fluss/server/log/FencedWriterAppendInfo.java`
- Modified `fluss-server/src/main/java/org/apache/fluss/server/log/WriterStateManager.java`
- Modified `fluss-server/src/test/java/org/apache/fluss/server/log/WriterStateManagerTest.java`
- Modified `fluss-server/src/test/java/org/apache/fluss/server/log/WriterSnapshotMapJsonSerdeTest.java`
- Added this report at `.superpowers/sdd/task-4-implementer-report.md`

`WriterStateEntry`, `WriterAppendInfo`, and all callers outside Task 4 were not changed.

## Self-Review

- V0 constructor, exact-next sequencing, five-batch history, rollover, expiration,
  corruption fallback, snapshot framing/write sequence, and public callers are retained.
- V1 stores one immutable latest entry per opaque full-width `WriterKey`; long sequences
  above `Integer.MAX_VALUE`, stale equal/lower values, and exact inclusive dominating
  target WAL offsets are tested.
- Prepared V1 updates capture current state, accept one append, do not mutate the map
  until `updateFenced`, and reject missing, superseded, cross-bucket, and repeated
  publication paths.
- V1 automatic expiry is a no-op. Predicate retirement updates `writerIdCount` and
  `isEmpty()` observes the selected map.
- V0-only and V1-only APIs guard protocol use before touching the unselected map.
- V2 serialization is exact version 2 with protocol version 1 and long fields. Parsing
  rejects version/protocol mismatch, missing or malformed fields, duplicate keys, and
  negative sequences. V0 IDs are never converted to `WriterKey`.
- V1 corruption is propagated with the snapshot preserved; Task 11 recovery fallback
  was not implemented. Existing V0 corrupt-snapshot deletion/fallback remains covered.
- `git diff --check` is clean, and the final code diff is limited to the five owned code
  and test files plus this report.

## Concerns

- The workspace-mandated `docs/SECONDARY_INDEX_ARCHITECTURE.md` was not present anywhere
  under the workspace. The supplied approved offset-fencing design was used as the
  architecture source.
- Spotless 2.27.1 fails under the active JDK 24 because its google-java-format integration
  calls a removed javac method. Formatting succeeded under installed JDK 17. The module
  formatter also touched unrelated already-unformatted files; those formatter-only
  changes were removed with targeted patches, leaving only Task 4 files changed.
- The reactor needs `-DfailIfNoTests=false` with a narrow `-Dtest` selector so aggregator
  modules without matching tests do not fail before `fluss-server` runs.
